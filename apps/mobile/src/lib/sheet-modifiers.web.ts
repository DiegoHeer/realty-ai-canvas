import type { ModifierConfig } from '@expo/ui/jetpack-compose/modifiers';

/** Web: `Row` already stretches (alignSelf: stretch) and ignores modifiers. */
export const FILL_WIDTH: ModifierConfig[] | undefined = undefined;
