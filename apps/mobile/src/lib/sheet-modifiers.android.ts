import { fillMaxWidth, type ModifierConfig } from '@expo/ui/jetpack-compose/modifiers';

/** Android: fill the parent width so the whole sheet row is tappable. */
export const FILL_WIDTH: ModifierConfig[] = [fillMaxWidth()];
